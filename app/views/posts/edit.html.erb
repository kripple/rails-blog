<div class="container">
	<%= form_for @post do |f| %>
		<%= render "form", { f: f } %>
		<p><%= f.submit "Save Changes", :class=>"submit-button" %></p>
	<% end %>

	<div class="box-container">
		<%= render @tags %>
	</div>

	<%= render "preview" %>
</div>