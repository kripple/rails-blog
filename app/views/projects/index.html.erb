<div class="container">
	<%= will_paginate @unpublished if logged_in? && @unpublished.length > 0 %>
	<%= will_paginate @projects %>
  <% if logged_in? %>
    <br>
		<p><%= link_to("New Project", new_project_path, :class=>"submit-button") %></p>
  <% end %>
</div>



