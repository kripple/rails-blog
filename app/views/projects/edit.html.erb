<div class="container">
	<%= form_for @project do |f| %>
		<%= render "form", { f: f } %>
		<p><%= f.submit "Save Changes", :class=>"submit-button" %></p>
	<% end %>

	<%= render "preview" %>
</div>


